for _, object in pairs(workspace:GetChildren()) do
    -- Ensure we don't try to delete internal objects that can't be destroyed
    if object:IsA("Model") or object:IsA("Part") or object:IsA("Folder") then
        object:Destroy()
    end
end
